# Fixture for build-index.test.ts. Entries are deliberately NOT sorted and mix
# every optional shape (card pointer, private visibility, canonical_paths) so
# the golden output locks the full v2 rendering surface (topology only —
# project state lives in each repository's project.v1.yaml card).
schema_version: libre-ai.repositories.v2
authority: fixture — not the real inventory
updated_on: 2026-07-24
repositories:
  - repository: libre-ai/zeta-product
    role: reserved-product-home
    card: project.v1.yaml
    layer: couche-1
    product: Zeta
    visibility: public
    lifecycle: active
    owns:
      - fixture product surface
    consumes:
      - hub contracts
    canonical_paths:
      - apps/zeta
  - repository: libre-ai/alpha-hub
    role: hub
    layer: moyeu
    visibility: public
    lifecycle: active
    owns:
      - fixture hub
    consumes: []
  - repository: libre-ai/product-research
    role: administrative-private
    layer: transverse
    visibility: private
    lifecycle: active
    owns:
      - fixture private administration
    consumes: []
